# Base URL for API endpoints
REACT_APP_API_BASE_URL=http://localhost:3000/api

# API request timeout in milliseconds
REACT_APP_API_TIMEOUT=5000

# Authentication Settings
# Token expiration time in seconds
REACT_APP_AUTH_TOKEN_EXPIRATION=3600

# Authentication Error Messages
# Error message for invalid login credentials
REACT_APP_AUTH_ERROR_INVALID_CREDENTIALS=Invalid username or password.

# Error message for expired authentication token
REACT_APP_AUTH_ERROR_TOKEN_EXPIRED=Authentication token has expired.

# Error message for unauthorized access attempts
REACT_APP_AUTH_ERROR_UNAUTHORIZED_ACCESS=You are not authorized to access this resource.

# HUMAN TASKS:
# 1. Copy this file to .env in the same directory
# 2. Update REACT_APP_API_BASE_URL to match your API deployment URL for non-local environments
# 3. Adjust REACT_APP_API_TIMEOUT if different timeout values are needed for your environment
# 4. Review and adjust REACT_APP_AUTH_TOKEN_EXPIRATION based on security requirements
# 5. Ensure these environment variables are properly set in your deployment pipeline